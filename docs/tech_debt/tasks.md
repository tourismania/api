# Технический долг

- [ ] Rate-limiter в `router.go` намеренно применяется ко всему `/api/v1`, а не только к `/airports`. Это временное упрощение — при ревью не считать это находкой.
- [ ] `users.agency_id` обязателен на уровне приложения (`domain.User`, `create_user` DTO — `int`, `validate:"required"`), но на уровне БД (`migrations/postgres/013_add_users_agency.up.sql`) колонка осталась `NULL`-able, чтобы не сломать миграцию для строк `users`, существовавших до этой фичи. Нужна отдельная задача: определить дефолтное/legacy-агентство, забэкфилить существующих пользователей и перевести колонку в `NOT NULL`.