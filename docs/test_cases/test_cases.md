# Тест-кейсы

Чек-лист функциональных сценариев по доменам. Готовые `curl`-команды для
HTTP-сценариев — в [`docs/curls/`](../curls/README.md) (один файл на группу
сценариев, см. таблицу соответствия там же).

## Domain

### Агенство

- [ ] Создание агенства
- [ ] Активация агенства
- [ ] Деактивация агенства

### Авторизация

- [ ] Логин с верными credentials → JWT
- [ ] Логин с неверным паролем → 401
- [ ] Логин с несуществующим email → 401 (тот же ответ, что и неверный пароль)

### Пользователь

- [ ] Получение информации об авторизованном пользователе
- [ ] Регистрация пользователя
- [ ] Регистрация с несуществующим/неактивным агентством → 400
- [ ] Регистрация с дублирующимся email

### Аэропорты

- [ ] Синхронизация аэропортов
- [ ] Поиск аэропортов по фильтру
- [ ] Поиск с `search` короче 2 символов → 400
- [ ] Поиск без токена → 401

### Offer (issue №12)

- [ ] Создание offer агентом/супер-админом своего агентства
- [ ] Создание offer с недостаточной ролью (`ROLE_USER`) → 403
- [ ] Список offer скоупится строго на агентство текущего пользователя, любая роль видит одно и то же
- [ ] Получение одного offer своего агентства (любой статус)
- [ ] Получение offer чужого агентства → 404 (не раскрываем существование)
- [ ] Публичная ссылка на `published` offer — без авторизации
- [ ] Публичная ссылка на `draft`/`ready` offer → 404
- [ ] Частичное обновление (`title`/`description`/`status`) — только переданные поля применяются
- [ ] Смена статуса `draft` → `ready` → `published`
- [ ] Soft delete offer своего агентства; повторное удаление → 404
- [ ] Запись/изменение/удаление offer чужого агентства → 404, даже для `ROLE_SUPER_ADMIN`

### Перелёты (Flight) в составе Offer (issue №20)

- [ ] Создание offer с одним прямым перелётом (без пересадки)
- [ ] Создание offer с перелётом с пересадкой (2+ сегмента) — offer и flights в одной транзакции
- [ ] Невалидная хронология сегмента (`arrival_at <= departure_at`) → 400, offer не создаётся
- [ ] Разрыв маршрута (аэропорт прилёта сегмента N ≠ аэропорту вылета сегмента N+1) → 400
- [ ] Пересадка нулевой/отрицательной длительности → 400
- [ ] Несуществующий `icao` в сегменте → 400
- [ ] `PATCH` без ключа `flights` — существующие перелёты не трогаются
- [ ] `PATCH` с `flights: []` — все перелёты офера удаляются
- [ ] `PATCH` с новым набором `flights` — старые полностью заменяются (diff-then-replace)
- [ ] `PATCH` с `flights`, идентичными уже сохранённым — no-op, БД не трогается
- [ ] `GET` детального offer возвращает вычисленные `total_duration_seconds` и `layovers` (0/1/2+ пересадок)
- [ ] `GET /offers` (список) не содержит `flights`
